---
title: "Filesystem layout"
sidebarTitle: "Filesystem layout"
description: "What lives under ~/.authsome. Every file, its purpose, and its permissions."
icon: "folder-tree"
keywords: ["authsome filesystem", "~/.authsome layout", "authsome home directory", "AUTHSOME_HOME"]
---

Authsome keeps all state under a single directory. The default is `~/.authsome/`; override it with the `AUTHSOME_HOME` environment variable.

## Tree

<Tree>
  <Tree.Folder name="~/.authsome/" defaultOpen>
    <Tree.File name="config.json" />
    <Tree.File name="master.key" />
    <Tree.File name="audit.log" />
    <Tree.Folder name="logs/">
      <Tree.File name="authsome.log" />
    </Tree.Folder>
    <Tree.Folder name="providers/">
      <Tree.File name="<name>.json" />
    </Tree.Folder>
    <Tree.Folder name="profiles/" defaultOpen>
      <Tree.Folder name="<profile_name>/">
        <Tree.File name="store.db" />
        <Tree.File name="lock" />
      </Tree.Folder>
    </Tree.Folder>
  </Tree.Folder>
</Tree>

## File reference

### `config.json`

Global settings. JSON, one object.

```json
{
  "spec_version": 1,
  "default_profile": "default",
  "encryption": {
    "mode": "local_key"
  }
}
```

| Field | Type | Notes |
|-------|------|-------|
| `spec_version` | int | Always `1`. |
| `default_profile` | string | The profile name authsome reads and writes by default. |
| `encryption.mode` | `"local_key"` or `"keyring"` | Where the master key lives. See [Encryption at rest](/security/encryption). |

### `master.key`

256-bit master key, base64-encoded JSON, mode `0600`. Present when `encryption.mode = "local_key"`. Absent when `encryption.mode = "keyring"` (the key lives in the OS keychain instead).

<Warning>
  Lose this file in `local_key` mode and every encrypted credential in the vault is unrecoverable. Back it up with the same care you'd give the credentials themselves.
</Warning>

### `audit.log`

Append-only newline-delimited log of significant actions: `login`, `logout`, `revoke`, `export`, `register`, `get --show-secret`. Each line records timestamp, action, provider, connection (where relevant), and outcome.

Read recent entries:

```bash
uvx authsome log
uvx authsome log -n 200
uvx authsome log --json
```

The schema is intentionally readable. The file is not encrypted; it's meant to be auditable.

### `logs/authsome.log`

Rotating loguru-managed debug log. Written when commands run with `--verbose` or when `LOGURU_LEVEL=DEBUG` is set. Inspect:

```bash
tail -f ~/.authsome/logs/authsome.log
```

The default path is `~/.authsome/logs/authsome.log`. Override with `--log-file <path>`. Pass `--log-file ""` to disable file logging entirely.

### `providers/<name>.json`

User-registered or user-overridden provider definitions. A file at this path with the same name as a bundled provider wins over the bundled one.

```bash
uvx authsome inspect github > ~/.authsome/providers/github.json
# edit fields
uvx authsome list   # github now shows source=custom
```

See [Provider schema](/reference/provider-schema) for the full schema and [Provider registry](/concepts/provider-registry) for resolution rules.

### `profiles/<name>/store.db`

SQLite credential vault for one profile. Holds:

- Per-provider metadata (known connections, default connection, last-used connection)
- Per-provider state (last refresh attempt and error)
- Per-provider OAuth `ProviderClientRecord` (`client_id`, encrypted `client_secret`)
- Per-connection `ConnectionRecord` (encrypted tokens / API key, scopes, expiry, account info)

Key namespace:

```text
profile:<profile>:<provider>:metadata
profile:<profile>:<provider>:state
profile:<profile>:<provider>:client
profile:<profile>:<provider>:connection:<connection_name>
```

Sensitive fields are encrypted at rest with AES-256-GCM. See [Credential storage](/concepts/credential-storage) and [Encryption at rest](/security/encryption).

### `profiles/<name>/lock`

Advisory `fcntl.flock` write lock for the profile. Multiple authsome processes can read the same profile concurrently but serialize writes within one profile. Locks across profiles are independent.

A stale `lock` from a crashed process can be deleted only when you're certain no other authsome process is running on this profile:

```bash
rm ~/.authsome/profiles/default/lock
```

## Sizing

Typical home directory after a few logins:

| Item | Size |
|------|------|
| `config.json` | < 1 KB |
| `master.key` | < 1 KB |
| `providers/<name>.json` | 1-3 KB each |
| `profiles/<name>/store.db` | ~50 KB per connection |
| `audit.log` | 200-400 B per event, grows linearly |
| `logs/authsome.log` | Rotates; bounded |

The vault is not large unless you accumulate hundreds of connections.

## Permissions

| Path | Default mode | Why |
|------|--------------|-----|
| `~/.authsome/` | `0700` | Only the user can read or traverse. |
| `~/.authsome/master.key` | `0600` | Read by the user only. `doctor` fails if this is more permissive. |
| `~/.authsome/profiles/<name>/store.db` | `0600` | Same justification. |
| `~/.authsome/audit.log` | `0600` | Same justification. |
| `~/.authsome/providers/*.json` | `0644` | Not sensitive on its own. |

`uvx authsome doctor` verifies the critical paths and their modes.

## Override the location

```bash
export AUTHSOME_HOME=/var/lib/authsome
uvx authsome whoami
```

Useful for:

- Ephemeral CI environments where you want a clean state per job.
- Per-project credential isolation by setting `AUTHSOME_HOME` in a `.envrc`.
- Hosted daemon deployments. See [Hosted deployment model](/security/hosted-deployment).

## What's next

<Columns cols={2}>
  <Card title="Credential storage" icon="database" href="/concepts/credential-storage">
    The vault key namespace and connection record shape.
  </Card>
  <Card title="Encryption at rest" icon="lock" href="/security/encryption">
    What's encrypted, with which algorithm, and where the key lives.
  </Card>
  <Card title="Environment variables" icon="terminal" href="/reference/environment-variables">
    `AUTHSOME_HOME` and friends.
  </Card>
  <Card title="Diagnose with doctor" icon="stethoscope" href="/troubleshooting/doctor">
    Verify the layout is healthy.
  </Card>
</Columns>
