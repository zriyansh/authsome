---
title: "NanoClaw, OpenClaw, HermesAgent"
sidebarTitle: "NanoClaw / OpenClaw / HermesAgent"
description: "Lightweight Python agent runners. Use authsome via the proxy or the library."
icon: "microchip"
keywords: ["nanoclaw authsome", "openclaw authsome", "hermesagent authsome", "lightweight agent credentials"]
---

NanoClaw, OpenClaw, and HermesAgent are lightweight Python agent runners in the agentr ecosystem. They don't bundle credential management. Authsome fills that gap with two interchangeable patterns. The same patterns work for any Python-native agent runner.

## Pattern 1: run under the proxy

```bash
uvx authsome login github
uvx authsome login openai
uvx authsome run -- python -m <your_runner>
```

The runner sees placeholder env vars. Outbound HTTPS to matched providers is authenticated at the proxy layer.

## Pattern 2: use the library

These runners are Python-native. Skip the proxy and call authsome directly from your agent code:

```python
from authsome.server.dependencies import create_auth_service

auth = create_auth_service()

github_token = auth.get_access_token("github")
openai_key = auth.get_access_token("openai")

# Pass to your runner's agent constructor however it expects credentials
# (env vars, kwargs, config object: runner-specific).
```

Refresh is silent. The agent never holds an expired token long enough to matter.

See [Python library](/reference/python-library) for the full surface.

## When to use which

| Use the proxy | Use the library |
|----------------|------------------|
| You want a single layer that catches every outbound call. | You want fine-grained control over which connection to use per agent. |
| You're using third-party clients you can't easily modify. | The agent is yours and you can pass tokens directly. |
| You need transparent injection for shell tools the agent spawns. | You don't spawn shell tools. |

## What's next

<Columns cols={2}>
  <Card title="Run agents with the proxy" icon="shield-halved" href="/guides/run-agents-with-proxy">
    The proxy injection model.
  </Card>
  <Card title="Python library" icon="code" href="/reference/python-library">
    `AuthService` and the auth-layer API.
  </Card>
</Columns>
