---
title: "Intercom"
sidebarTitle: "Intercom"
description: "Store and use your Intercom API key with authsome. Local encrypted vault, proxy injection, no key in the agent's environment."
icon: "intercom"
keywords: ["intercom api key authsome", "intercom key broker", "intercom agent credentials", "authsome intercom"]
---

import ProxyInjection from "/snippets/proxy-injection.mdx";
import MultiConnectionsCta from "/snippets/multi-connections-cta.mdx";
import MaskedInputNote from "/snippets/masked-input-note.mdx";
import WhatsNextApiKey from "/snippets/whats-next-apikey.mdx";

Intercom uses a long-lived API key. Authsome stores the key encrypted in the local vault, injects it at request time through the proxy, and keeps it out of shell history, process listings, and environment dumps.

## At a glance

| | |
|---|---|
| Provider name | `intercom` |
| Display name | Intercom |
| Auth type | API key |
| Header | `Authorization: Bearer <key>` |
| Proxy host | `api.intercom.io` |
| Env var | `INTERCOM_API_KEY` |
| Where to get a key | [https://app.intercom.com/a/apps/_/developer-hub/access-tokens](https://app.intercom.com/a/apps/_/developer-hub/access-tokens) |

## Get a key

Create an API key in the Intercom dashboard at [https://app.intercom.com/a/apps/_/developer-hub/access-tokens](https://app.intercom.com/a/apps/_/developer-hub/access-tokens).

## Log in

```bash
uvx authsome login intercom
```

A local browser form opens at `http://127.0.0.1:7999`. Paste the key into the masked input and submit.

<MaskedInputNote />

Verify:

```bash
uvx authsome get intercom --field status
# → connected
```

## Use the key

<ProxyInjection envVar="INTERCOM_API_KEY" secretLabel="key" host="api.intercom.io" />

### From a Python agent

```python
from authsome.context import AuthsomeContext

ctx = AuthsomeContext.create()
key = ctx.auth.get_access_token("intercom", connection="default")
```

### As an environment variable

```bash
eval "$(uvx authsome export intercom --format env)"
echo $INTERCOM_API_KEY
```

## Multiple keys

<MultiConnectionsCta />

```bash
uvx authsome login intercom --connection personal
uvx authsome login intercom --connection team
```

## Rotate or remove the key

```bash
uvx authsome login intercom --force          # rotate
uvx authsome logout intercom                  # remove local credential
uvx authsome remove intercom                  # remove all local state for the provider
```

API-key providers have no revocation endpoint, so `revoke` and `remove` are equivalent for Intercom.

## Override the bundled definition

```bash
uvx authsome inspect intercom > ~/.authsome/providers/intercom.json
# edit fields
uvx authsome list   # source now shows "custom" for intercom
```

See [Provider schema](/reference/provider-schema) for every field.

## What's next

<WhatsNextApiKey />
