---
title: "Beehiiv"
sidebarTitle: "Beehiiv"
description: "Store and use your Beehiiv API key with authsome. Local encrypted vault, proxy injection, no key in the agent's environment."
keywords: ["beehiiv api key authsome", "beehiiv key broker", "beehiiv agent credentials", "authsome beehiiv"]
---

import ProxyInjection from "/snippets/proxy-injection.mdx";
import MultiConnectionsCta from "/snippets/multi-connections-cta.mdx";
import MaskedInputNote from "/snippets/masked-input-note.mdx";
import WhatsNextApiKey from "/snippets/whats-next-apikey.mdx";

Beehiiv uses a long-lived API key. Authsome stores the key encrypted in the local vault, injects it at request time through the proxy, and keeps it out of shell history, process listings, and environment dumps.

## At a glance

| | |
|---|---|
| Provider name | `beehiiv` |
| Display name | Beehiiv |
| Auth type | API key |
| Header | `Authorization: Bearer <key>` |
| Proxy host | `api.beehiiv.com` |
| Env var | `BEEHIIV_API_KEY` |
| Where to get a key | [https://app.beehiiv.com/settings/integrations/api](https://app.beehiiv.com/settings/integrations/api) |

## Get a key

Create an API key in the Beehiiv dashboard at [https://app.beehiiv.com/settings/integrations/api](https://app.beehiiv.com/settings/integrations/api).

## Log in

```bash
uvx authsome login beehiiv
```

A local browser form opens at `http://127.0.0.1:7998`. Paste the key into the masked input and submit.

<MaskedInputNote />

Verify:

```bash
uvx authsome get beehiiv --field status
# → connected
```

## Use the key

<CodeGroup>

```python Python library
from authsome.server.dependencies import create_auth_service

auth = create_auth_service()
key = auth.get_access_token("beehiiv", connection="default")
```

```bash Proxy
uvx authsome run -- python my_agent.py
```

```bash Environment
eval "$(uvx authsome export beehiiv --format env)"
echo $BEEHIIV_API_KEY
```

</CodeGroup>

Under the proxy, authsome sets `BEEHIIV_API_KEY=authsome-proxy-managed` in the child's environment and injects the real key into outbound requests to `api.beehiiv.com`. The child process never sees the actual value.

## Multiple keys

<MultiConnectionsCta />

```bash
uvx authsome login beehiiv --connection personal
uvx authsome login beehiiv --connection team
```

## Rotate or remove the key

```bash
uvx authsome login beehiiv --force          # rotate
uvx authsome logout beehiiv                  # remove local credential
uvx authsome remove beehiiv                  # remove all local state for the provider
```

API-key providers have no revocation endpoint, so `revoke` and `remove` are equivalent for Beehiiv.

## Override the bundled definition

```bash
uvx authsome inspect beehiiv > ~/.authsome/providers/beehiiv.json
# edit fields
uvx authsome list   # source now shows "custom" for beehiiv
```

See [Provider schema](/reference/provider-schema) for every field.

## What's next

<WhatsNextApiKey />
